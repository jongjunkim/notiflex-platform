apiVersion: v1
kind: Service
metadata:
  name: notiflex-api
  namespace: notiflex
  labels:
    app: notiflex-api
    app.kubernetes.io/part-of: notiflex
spec:
  type: ClusterIP
  selector:
    app: notiflex-api
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
